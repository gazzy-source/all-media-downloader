python-telegram-bot[job-queue]==21.10
yt-dlp>=2025.1.15
# Chrome-impersonation for yt-dlp (needed for cookieless Instagram/Facebook).
# Pin: yt-dlp requires curl-cffi >=0.5.10,<0.16 — 0.15.0 is its tested version.
curl_cffi==0.15.0
# YouTube PO-token provider plugin (needs a bgutil provider server running —
# see docker-compose.yml). Lets yt-dlp download WITHOUT cookies.
bgutil-ytdlp-pot-provider>=1.0.0
aiofiles>=24.1.0
aiohttp>=3.11.0
python-dotenv>=1.0.1
Pillow>=11.0.0
mutagen>=1.47.0
psutil>=6.1.0
